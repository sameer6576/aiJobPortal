# Secrets for Compose. For IntelliJ, also copy local-native.env.example or
# local-hybrid.env.example — Config Server does not default DB host/port.
DB_PASSWORD=
JWT_SECRET=
GEMINI_API_KEY=
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
