spring:
  application:
    name: job-portal-api-gateway
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

app:
  jwt:
    secret: test-jwt-secret-for-local-tests-only-0123456789
